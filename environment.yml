# conda env create -f environment.yml
name: ctlearn
channels:
    - anaconda
    - conda-forge
    - cta-observatory
    - ctlearn-project
dependencies:
    - python=3.10
    - astropy
    - matplotlib
    - numpy
    - pandas
    - pip
    - pyyaml
    - scikit-learn
    - ctapipe
    - pip:
        - ctaplot
        - numba
        - tensorflow>=2.14,<2.15
        - dl1_data_handler>=0.13.0,<0.14
        - pydot
        - pyirf
