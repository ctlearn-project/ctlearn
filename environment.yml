# conda env create -f environment.yml
name: ctlearn
channels:
    - anaconda
    - conda-forge
dependencies:
    - python=3.11
    - astropy>=7.0.1,<0.8
    - setuptools
    - numpy
    - pandas
    - pip
    - pyyaml
    - scikit-learn
    - ctapipe
    - pip:
        - numba
        - tensorflow>=2.14,<2.15
        - dl1_data_handler>=0.14.1,<0.15
        - pydot
