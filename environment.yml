# conda env create -f environment.yml
name: ctlearn
channels:
    - anaconda
    - conda-forge
dependencies:
    - python=3.11
    - setuptools
    - numpy
    - pandas
    - pip
    - pyyaml
    - scikit-learn
    - ctapipe
    - pip:
        - astropy
        - numba
        - tensorflow>=2.14,<2.15
        - dl1_data_handler>=0.14.1,<0.15
        - pydot
