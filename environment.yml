# conda env create -f environment.yml
name: ctlearn
channels:
    - anaconda
    - conda-forge
dependencies:
    - python=3.10
    - astropy
    - setuptools
    - numpy
    - pandas
    - pip
    - pytables
    - pyyaml
    - scikit-learn
    - ctapipe
    - pip:
        - numba
        - tensorflow>=2.14,<2.15
        - dl1_data_handler>=0.14.5,<0.15
        - pydot
